# PhasePaint
Official Repo of the PhasePaint Project

start gradio app with `python app.py`

Specify USER in `config.py` for logging purposes.
Images saved to `saved_imgs/{USER}`
logs saved in `logs/{USER}_activity`